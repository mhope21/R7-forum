<% if subscription.id
  url = subscription_path(subscription)
  else 
  url = forum_subscriptions_path(subscription.forum)
  Rails.logger.debug "Subscription.forum = #{subscription.forum.inspect}"
  end %>
<%= form_with(model: subscription, url: url) do |form| %>
  <% if subscription.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subscription.errors.count, "error") %> prohibited this subscription from being saved:</h2>

      <ul>
        <% subscription.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <h2><%= "Subscription for forum #{subscription.forum.forum_name}." %></h2>

  <div>
    <h2><%= "Priority level 1 - 10." %></h2>
    <%= form.label :priority, style: "display: block" %>
    <%= form.number_field :priority, min: 1, max: 10 %>

  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
